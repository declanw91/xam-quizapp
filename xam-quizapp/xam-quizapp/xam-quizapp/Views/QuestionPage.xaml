<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="quizapp.Views.QuestionPage">
    <ContentPage.Content>
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <StackLayout Grid.Row="0">
                <StackLayout Orientation="Horizontal">
                    <Label Text="Question" />
                    <Label Text="{Binding CurrentQuestionNumber}" />
                    <Label Text="out of" />
                    <Label Text="{Binding TotalQuestions}"/>
                </StackLayout>
            </StackLayout>
            <StackLayout Grid.Row="1">
                <Frame Style="{DynamicResource QuestionStyle}">
                    <StackLayout Orientation="Horizontal" Style="{DynamicResource QuestionStyle}">
                        <Image Source="circledq.png"/>
                        <Label Text="{Binding CurrentQuestion.Question}" />
                    </StackLayout>
                </Frame>
            </StackLayout>
            <StackLayout Grid.Row="2">
                <ListView x:Name="lvAnswers" ItemsSource="{Binding CurrentQuestion.Answers}" SelectedItem="{Binding UserAnswer}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <Label Text="{Binding .}" />
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackLayout>
            <StackLayout Grid.Row="3">
                <Button Text="Submit Answer" Command="{Binding SubmitAnswer}"/>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>